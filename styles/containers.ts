import { StyleSheet } from 'react-native';

export const container = StyleSheet.create({
  container: {
    flex: 1,
  },
  safe: {
    paddingTop: 55,
  },
  dark: {
    backgroundColor: '#191919',
  }
})